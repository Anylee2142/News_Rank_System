1. User <-> View <-> Article (done)

2. Article은 폴더에 크롤링한 애들을 넣음 (done)
(기사내용은 BLOB)

3. User, view는 seed를 만듦 (done)

4. 얘네를 바탕으로 similar matrix를 만듦 (done)

5. 추천 (done)

6. 추천잘했는지 안했는진 어떻게 측정할까..

---------------------------------------------

1. 접속한 User를 Fetch

2. 해당 유저의 genre vector를 계산

3. 이 genre vector에 따라 뉴스검색 결과의 순위를 바꿔서 웹페이지를 띄움

4. 뉴스검색결과는 네이버뉴스api를 사용, 장르구분은 훈련한 모델로 predict

---------------------------------------------

1. 문서 전체에 공통으로 나오는 단어들은 stop_words로 처리

2. set intersection으로 처리

---------------------------------------------

1. 뉴스검색을 위한 웹페이지

2. 1번에다 추천기사 테이블 붙임

---------------------------------------------

1. tf-idf gridsearch

2. 실시간으로 크롤링해서 디비에 저장하는?